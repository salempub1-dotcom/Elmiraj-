name: Patch ZR Validation

on:
  push:
    branches: [feature/zrexpress-integration]
    paths:
      - 'scripts/patch-zr-validation.mjs'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feature/zrexpress-integration
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply validation patch
        run: node scripts/patch-zr-validation.mjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/deliveryProviders.js
          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "fix: surface ZR validation errors and normalize parcel payload"
            git push origin feature/zrexpress-integration
          fi
