# صور الـHero — أضف الملفين هنا بهذين الاسمين بالضبط

ضع هنا صورتَي Hero الحقيقيتَين. الكود في `src/components/home/HeroVisual.tsx` يبحث عنهما
تلقائيًا بهذين المسارين، وسيعرضهما فور توفرهما بدون أي تعديل إضافي على الكود:

1. `hero-teacher.webp` — صورة أستاذة داخل القسم تستخدم وسيلة تعليمية من المعراج (اللوحة
   الرئيسية الكبيرة). يُفضَّل مقاس عمودي تقريبًا 1000×1250px.
2. `hero-products.webp` — صورة حقيقية لمنتجات Al Miraj Education (Flash Cards / Lesson
   Plans...) (اللوحة الأصغر المتراكبة). يُفضَّل مقاس أفقي تقريبًا 800×500px.

استخدم صيغة WebP مضغوطة للحفاظ على أداء الصفحة. طالما الملفان غير موجودين، يظهر تكوين
بصري بديل (Placeholder) بهوية الموقع بدل الصورتين.

## صور قسم "شاهد وسائل المعراج داخل القسم"

نفس المبدأ (`src/components/home/ClassroomShowcaseSection.tsx`):

3. `classroom-use-1.webp` — صورة أستاذة تستعمل Flash Cards داخل القسم (الصورة الكبيرة).
   يُفضَّل مقاس عمودي تقريبًا 1000×1250px.
4. `classroom-use-2.webp` — Close-up لمنتجات المعراج أثناء الاستعمال (الصورة الأصغر
   المتراكبة). يُفضَّل مقاس أفقي تقريبًا 900×675px.

هذا الملف (README.md) للتوثيق فقط ولا يؤثر على الموقع — يمكن حذفه لاحقًا إن أردت.
