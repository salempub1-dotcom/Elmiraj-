// Updated notification message template to use customerWilayaLabel instead of customerWilaya.

// Original code(segment around line 356)
const notificationMessage = `${customerWilaya} has completed the action.`;

// Updated code
const notificationMessage = `${customerWilayaLabel} has completed the action.`;